# database/ — Capa Oracle

Objetos en **inglés** (SB-04).

## Carpetas

- `00_config` — plantillas de conexión (sin secretos en Git)
- `01_schema` — tablas, constraints, índices
- `02_seed` — institución, usuario demo
- `03_checks` — verificación

## Entidades

`user_account`, `institution`, `staff_member`, `staff_institution`,  
`biometric_import`, `biometric_mark`, `inconsistency`, `justification`,  
`attendance_day` (**fuente de verdad**), `audit_log`
